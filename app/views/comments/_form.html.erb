<%= form_for [topic, post, post.comments.new] do |f| %>


  <%= f.label :body %>
<%= f.text_area :body, class: 'form-control', placeholder: "Enter Comment Body" %>

<div class="form-group">
  <%= f.submit "Save", class: 'btn btn-success' %>
</div>
<% end %>
